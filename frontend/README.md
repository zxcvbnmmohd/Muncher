# The Frontend of Muncher

Organise imports
Copy over packges to requirements.txt
```shell
importanize src/main.py
```

Copy over packges to requirements.txt
```shell
pipenv run pip freeze > requirements.txt
```
